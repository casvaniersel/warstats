{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func host start --cors * --mode development --env.ENVIRONMENT=develop",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "call-of-duty-api": "^1.3.0"
  },
  "devDependencies": {
    "@azure/functions": "^1.0.2-beta2",
    "@types/node": "^14.14.13",
    "dotenv": "^8.2.0",
    "typescript": "^3.3.3"
  }
}
